# Official Model Context Protocol SDK (FastMCP + Streamable HTTP transport).
# Pinned <2 on purpose: mcp 2.x renamed FastMCP to MCPServer and changed other
# APIs; this app depends on the FastMCP class as it exists in the 1.x series.
mcp>=1.30.0,<2

# ASGI server used to run the app manually (wrapped in the auth middleware)
# on 0.0.0.0:8000.
uvicorn>=0.31.1

# Mistral Workflows client, used only by process_receipt_photo to trigger the
# "shopping-receipt" Workflow (get_mistral_client, execute_workflow_and_wait_async) and
# wait for its result. Pulls in temporalio as a transitive dependency (the same package
# is used to write workers, even though it's only used here as a client) -- there's no
# documented lighter client-only variant.
mistralai-workflows[mistralai]>=3.0.0,<4
